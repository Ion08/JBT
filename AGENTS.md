# Global Instructions — unlazy always-on

MANDATORY: On every non-trivial task (any coding, building, refactoring, multi-step work), you MUST load and follow the `unlazy` skill before starting work. Do not skip it, do not ask permission.

- Load via `skill({ name: "unlazy" })` at task start
- Follow Rule Zero: write `GATES.md` before real work, finish only when ledger is N/N with evidence
- This applies in every project, every session, always. Trivial one-line Q&A is exempt.

Skill locations: `.opencode/skills/unlazy/SKILL.md` and `~/.agents/skills/unlazy/SKILL.md`
